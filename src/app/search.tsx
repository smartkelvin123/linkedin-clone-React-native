import { StyleSheet, Text, View } from "react-native";

const Search = () => {
  return (
    <View>
      <Text>searc</Text>
    </View>
  );
};

export default Search;

const styles = StyleSheet.create({
  container: {},
});
